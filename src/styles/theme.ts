import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  typography: {
    fontFamily: 'Poppins, sans-serif',
  },
  palette: {
    primary: { main: '#10069F' },
    secondary: { main: '#FFB641' },
    // success: { main: '#37c49f' },
    info: { main: '#263238' },
    // error: { main: '#ef426f' },
  },
})
